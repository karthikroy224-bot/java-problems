class Solution {
    public int[] sortedSquares(int[] nums) {
        int inx=0;
        for(int i=0;i<nums.length;i++){
            nums[inx++]=(nums[i])*(nums[i]);
        }
        Arrays.sort(nums);
        return nums;
    }
}
